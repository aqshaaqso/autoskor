VITE_API_BASE_URL=http://172.16.210.244:8000/api

# Dokumen / scoring-jobs — false = pakai middleware API
VITE_USE_MOCK=false

# Auth & admin — tetap mock (middleware belum punya /auth/* dan /admin/*)
VITE_USE_MOCK_AUTH=true
VITE_USE_MOCK_ADMIN=true

# Engine — false = data dari GET /scoring-jobs (tidak ada /engine/status di middleware)
VITE_USE_MOCK_ENGINE=false

VITE_SCORING_JOBS_LIST_LIMIT=100